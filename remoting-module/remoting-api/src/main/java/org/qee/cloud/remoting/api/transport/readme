1、transport 作为传输层，RPC调用是点对点通信，则抽象一个端点EndPoint,它应该对应有如何几个方法：
    getUrl()用来描述这个端点
    getLocalAddress()描述端点的地址
2、 在网络通信中，客户端client 发送请求给服务端server,或者服务端发送响应给客服端。
其扮演的角色是可以相互转换的，称为对等端Peer.则构建一个AbstractPeer.
3、在rpc调用中，我们把服务提供方称为Server端，把服务调用方称为Client端，其在服务器上本身又是对等端。
   则分别创建AbstractClient,AbstractServer.继承自AbstractPeer,并申明Client,Server接口，代表其客户端/服务端语义
4、客户端和服务端本身是一个终端，所有通过实现一个AbstractEndPoint（继承自对等端）来统一客户端和服务端。
5、一般认为rpc调用是远程服务端，所以我们抽象RemotingServer接口(extends Server),仅做标识

以下则是各个模型相应属性归属，每一层所做的事情
